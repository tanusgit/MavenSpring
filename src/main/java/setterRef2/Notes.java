package setterRef2;

public class Notes {
/*
 * 3 classes:
DAO - > username (string)+ password(string) as dependencies
AccountService ->  value(string) + dao (obj) as dependencies
UserService ->  data(string) + dao (obj) as dependencies
req: cal the m1() and m2() methods.
 */
}
